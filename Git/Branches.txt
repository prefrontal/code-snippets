// -- Git Branches -----------------------------------------------------------------------

# List branches

	git branch
	git branch -av
	
# Create new branch (based on HEAD)

	git branch <name>

# Switch HEAD branch

	git checkout <name>
	
# Delete a local branch

	git branch -d <branch>

# Merge changes from a branch

	git merge <name>